import { BarValueDirective } from './bar-value.directive';

describe('BarValueDirective', () => {
  it('should create an instance', () => {
    const directive = new BarValueDirective();
    expect(directive).toBeTruthy();
  });
});
